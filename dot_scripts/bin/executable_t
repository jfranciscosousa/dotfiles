#!/usr/bin/env bash
set -euo pipefail

# t - Touch with mkdir. Creates parent directories and the file in one step.

if [ "$#" -eq 0 ]; then
  echo "Usage: $0 <filename>"
  exit 1
fi

mkdir -p "$(dirname -- "$1")"

touch "$1"
