#!/bin/bash
# y - Universal JS package manager. Delegates to yarn, npm, or pnpm based on lockfile.

if [ -f "yarn.lock" ]; then
  command="yarn"
elif [ -f "package-lock.json" ]; then
  command="npm"
else
  command="pnpm"
fi

eval "$command $@"
