#!/usr/bin/env bash
set -euo pipefail

source_dir=$(chezmoi source-path)
cd "$source_dir"

exec opencode run --auto --title "tooling update" \
  "Use the francisco-tooling-update skill and execute its complete workflow."
