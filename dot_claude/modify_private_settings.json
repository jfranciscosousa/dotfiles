{{- /* chezmoi:modify-template */ -}}
{{- $existing := dict -}}
{{- if .chezmoi.stdin -}}
{{- $existing = .chezmoi.stdin | fromJson -}}
{{- end -}}
{{- if hasKey $existing "hooks" -}}
{{- $_ := unset (index $existing "hooks") "UserPromptSubmit" -}}
{{- $_ := unset (index $existing "hooks") "SubagentStart" -}}
{{- end -}}

{{- $managed := dict
  "env" (dict
    "ENABLE_LSP_TOOL" "1"
    "CLAUDE_CODE_SHELL" "/bin/zsh"
  )
  "permissions" (dict
    "defaultMode" "auto"
  )
  "statusLine" (dict
    "type" "command"
    "command" "bash ~/.claude/statusline-command.sh"
  )
  "effortLevel" "xhigh"
  "tui" "fullscreen"
  "preferredNotifChannel" "terminal_bell"
  "skipAutoPermissionPrompt" true
  "hooks" (dict
    "PreToolUse" (list
      (dict
        "matcher" "Bash"
        "hooks" (list
          (dict
            "type" "command"
            "command" "rtk hook claude"
          )
        )
      )
    )
  )
-}}

{{ mergeOverwrite $existing $managed | toPrettyJson }}
